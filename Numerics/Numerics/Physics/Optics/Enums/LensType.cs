namespace CSharpNumerics.Physics.Optics.Enums;

/// <summary>Type of thin lens.</summary>
public enum LensType
{
    /// <summary>Converging (positive focal length).</summary>
    Converging,

    /// <summary>Diverging (negative focal length).</summary>
    Diverging
}
